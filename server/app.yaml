runtime: custom
vm: true
api_version: 1

manual_scaling:
  instances: 1

handlers:

- url: /(.*\.(css|js|json|ico|png|map))
  static_files: src/\1
  upload: src/(.*\.(css|js|json|ico|png|map))
  secure: always

- url: /.*
  script: app.js
  secure: always
